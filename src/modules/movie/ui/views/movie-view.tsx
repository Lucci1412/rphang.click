import React from "react";

import MovieListSession from "../session/movie-list-session";

const MovieView = () => {
  return <MovieListSession></MovieListSession>;
};

export default MovieView;
